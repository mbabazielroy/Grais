OPENAI_API_KEY=your-api-key-or-dummy-token
# To use your own model, set LLM_PROVIDER=local for the built-in deterministic scorer
# or point to any OpenAI-compatible endpoint (self-hosted):
LLM_PROVIDER=local
# LLM_BASE_URL=http://localhost:8000/v1
# LLM_MODEL=your-model-name
